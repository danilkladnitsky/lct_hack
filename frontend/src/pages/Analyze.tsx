import React from 'react';
import withLayout from 'hoc/withLayout';
import { AppLayout } from 'layouts';

const Analyze = () => {
  return (
    <div>Analyze</div>
  );
};

export default withLayout(AppLayout, <Analyze />);
